# Hollow Block Grading Classifier

This app uses a MobileNet-based model to classify hollow blocks into quality grades.

## How to run locally
```bash
pip install -r requirements.txt
streamlit run app.py
